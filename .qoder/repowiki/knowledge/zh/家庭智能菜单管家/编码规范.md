- UI 组件采用 shadcn/ui 模式，通过 `class-variance-authority` (cva) 定义变体，并使用 `cn` 工具函数合并 Tailwind 类名。
- 状态管理遵循 Zustand 模式，Store 文件中直接定义异步 Actions 并内部调用 Dexie 数据库方法进行持久化。
- 核心算法与 UI 解耦，菜单生成逻辑独立封装在 `src/algorithms/` 中，通过纯函数或简单上下文对象接收数据并返回结果。
- 数据库初始化采用懒加载或启动时检查策略，若 `recipes` 表为空则自动导入 `seed.ts` 中的预设数据。